// Configure your admin dashboard
window.CURBEASE_ADMIN = {
  // Publish your Google Sheet to the web (File → Share → Publish to web → CSV link) and paste the URL here:
  SHEET_CSV_URL: "https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/export?format=csv",
  // Deploy the Apps Script Web App with doPost(e) from docs/sheets_update_endpoint.gs and paste its URL here:
  // Set a shared secret in Apps Script and match it here for simple auth:
  };